/**
 * This tells TypeScript that importing CSS files is allowed.
 *
 * Example:
 * import "./styles.css";
 *
 * Without this declaration, TypeScript may show an error
 * because it does not know how to handle ".css" files.
 */
declare module "*.css";